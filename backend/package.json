{
  "name": "backend",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint --ext .ts src",
    "build": "tsc",
    "serve": "pnpm run build && firebase emulators:start --only auth,functions,firestore,database,pubsub",
    "deploy": "pnpm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "12",
    "firebase-functions": "6.3.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^7",
    "@typescript-eslint/parser": "^7",
    "eslint": "8.57.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.29.1",
    "typescript": "^5.4.0"
  }
}